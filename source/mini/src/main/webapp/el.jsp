
				<h1>${helloEL}</h1>
				<h1>${student.name}</h1>
				<!-- 
				${pageScope.student}
				${requestScope.student}
				${sessionScope.student}
				${applicationScope.student}
				${1}
				${1.1}
				${1E}
				${'abc'}
				${"abc"}
				
				${1+2}
				${1-2}
				${1*2}
				${1/2}
				${1 div 2}
				${1%2  == 1}
				${1 mod 2}
				
				>
				<
				>=
				<=
				null
				empty
				
				gt
				lt
				
				
				request.getParameter("name");
				
				param.name
				
				
				<c:choose>
					<c:when test="${student.score >= 90}">
						<jsp:forward page="great.jsp" />
					</c:when>
					<c:when test="${student.age >= 75}">
						<jsp:forward page="well.jsp" />
					</c:when>
					<c:when test="${student.age >= 60}">
						<jsp:forward page="success.jsp" />
					</c:when>
					<c:otherwise>
						<jsp:forward page="fail.jsp" />
					</c:otherwise>
				</c:choose>
				 -->
				